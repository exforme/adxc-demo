# -----------------------------------------------------------------------------
# MIQM Control operation bindings
# -----------------------------------------------------------------------------
# Location: profiles/<PROFILE>/operations/control.conf
# Type values: none, command, script
# Ref values:
#   command -> command object name, for example demo-start-node-command
#   script  -> scripts/name.sh, profiles/<PROFILE>/scripts/name.sh, or /absolute/path.sh

CONTROL_START_NODE_TYPE="none"
CONTROL_START_NODE_REF=""
CONTROL_STOP_NODE_TYPE="none"
CONTROL_STOP_NODE_REF=""
CONTROL_FAILOVER_TYPE="none"
CONTROL_FAILOVER_REF=""
