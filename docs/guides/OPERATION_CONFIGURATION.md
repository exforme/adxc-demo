# Operation Configuration

Profile operation configuration resides in:

```text
profiles/<PROFILE>/operations/
```

Each menu has its own file:

```text
control.conf
troubleshooting.conf
checks.conf
maintenance.conf
```

Operations are profile-level bindings. They are not hard-coded in dashboards.
